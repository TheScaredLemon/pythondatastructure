def last_element(lst):
    """Return last item in list (None if list is empty.
    
        >>> last_element([1, 2, 3])
        3
        
        >>> last_element([]) is None
        True
    """
    
    def last_element(1st):
        if len(1st) ==0:
            return None
        else:
            last_item = 1st[-]
            return last_item